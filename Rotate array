class solution:
    def rotate_array(arr, k):
        n = len(arr)
        k = k % n
        arr = arr[-k:] + arr[:k]
        return arr
        arr = [1, 2, 3, 4, 5, 6, 7]
        k = 3
        print("original Array:", arr)
        print("rotate                array:", rotate_array(arr, k))
