class Solution:
    def missingNum(self, arr):
        n = len(arr) + 1

        total = n * (n + 1) // 2
        arr_sum = sum(arr)

        return total - arr_sum


arr = [1, 2, 4, 5]

obj = Solution()
print("Missing Number:", obj.missingNum(arr))
        
